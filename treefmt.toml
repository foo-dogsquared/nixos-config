[global]
excludes = [
  "pkgs/firefox-addons/default.nix"
]

[formatter.lua]
command = "stylua"
includes = [ "*.lua" ]

[formatter.nix]
command = "nixpkgs-fmt"
includes = [ "*.nix" ]

[formatter.python]
command = "black"
includes = [ "*.py" ]

[formatter.terraform]
command = "terraform"
options = [ "fmt" ]
includes = [ "*.tf" ]
