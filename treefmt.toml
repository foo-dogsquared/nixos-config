[global]
excludes = [
  "pkgs/firefox-addons/default.nix"
]

[formatter.lua]
command = "stylua"
includes = [ "*.lua" ]

[formatter.nix]
command = "nixfmt"
includes = [ "*.nix" ]

[formatter.python]
command = "black"
includes = [ "*.py" ]

[formatter.terraform]
command = "tofu"
options = [ "fmt" ]
includes = [ "*.tf" ]
